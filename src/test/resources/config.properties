# Database Configuration
db.url=jdbc:postgresql://localhost:5432/prova_tecnica_qa
db.username=postgres
db.password=postgres
db.driver=org.postgresql.Driver

# Application URLs
app.base.url=http://localhost:8080
api.base.url=http://localhost:8080/api

# Browser Configuration
browser=chrome
headless=false
maximize=true

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=20
page.load.timeout=30
script.timeout=30

# Test Users
user.admin.username=admin
user.admin.password=admin123

user.regular.username=user
user.regular.password=user123

user.visitor.username=visitor
user.visitor.password=visitor123

user.blocked.username=blocked_user
user.blocked.password=any_password

user.invalid.username=invalid
user.invalid.password=wrong

# API Configuration
api.content.type=application/json
api.timeout=10000

# Retry Configuration
retry.count=3
retry.delay=1000
